# resources

Bundled app assets (icons, tray images, fonts, info messages).

The `electron-builder.yml` picks up files from this directory at package time.

- `icon.ico` — Windows app icon (also serves as tray icon)
- `info-messages.json` — bundled Info Section content (rotating messages)
