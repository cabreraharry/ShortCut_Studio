{
  "name": "scl_admin",
  "version": "1.0.0",
  "description": "Windows Folder Application",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node start.js && electron .",
    "//stop": "node stop.js",
    "package-win": "electron-packager . SCL_Admin --platform=win32 --arch=x64 --out=release-builds --overwrite --icon=icon.ico"
  },
  "dependencies": {
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "electron": "23.3.13",
    "electron-builder": "24.13.3",
    "electron-packager": "*",
    "electron-rebuild": "^3.2.9"
  }
}
