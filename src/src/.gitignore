node_modules/
out/
dist/
release-builds/
*.log
.DS_Store
.vite/

# SQLite DBs and backups — data, not code
db_files/*.db
db_files/*.backup.*.db

# Env
.env
.env.local
.env.*.local
tsconfig.*.tsbuildinfo

# Storybook screenshots — regenerated by `npm run storybook`
storybook/screenshots/

# Vendor binaries — fetched on demand by scripts/fetch-vendor-binaries.mjs.
# IPFS Kubo (~41 MB) and Nginx (~2 MB) get bundled into the installer via
# electron-builder.yml::extraResources. Do not commit; refresh by bumping the
# version constants in the fetcher and re-running `npm run fetch-vendor`.
vendor/
